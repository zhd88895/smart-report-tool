# 功能更新说明（2026-08-04）

> 面向项目接手人。本文档汇总 2026-08-04 完成的三组功能改动：上传去重（秒传）与临时文件保留清理、AI 支持包整包智能分析、AI 分析双模式开关与用户补充提示。
> 仓库结构：后端 `smart-report-server/`（Express + sqlite3 + tsx），前端 `smart-report-tool/`（React + zustand + Vite），两者为独立 git 仓库，均直接提交 master。

---

## 一、上传去重（秒传）与临时文件保留清理

### 功能说明

- **秒传**：用户上传的文件先在前端计算完整 SHA-256，调用后端预检查；若服务端已存在相同内容，则跳过上传，仅传 hash 引用。
- **保留清理**：后端定时清理超过保留天数的临时上传文件，保留天数由系统设置 `storage.retentionDays`（默认 90 天）控制，在系统设置页可改，下个清理周期生效。

### 架构与关键文件

| 层 | 文件 | 说明 |
|---|---|---|
| 后端 | `src/services/fileDedupService.ts` | 内容寻址存储（CAS）：文件以 `<sha256><扩展名>` 存于 `data/uploads/dedup/`，索引在 sqlite `file_hashes` 表（含 `last_used_at`，清理依据） |
| 后端 | `src/services/fileCleanupService.ts` | 清理调度：启动 1 分钟后及每 6 小时运行；CAS 按 `last_used_at`、uploads 根目录散落文件按 mtime 判定 |
| 后端 | `src/routes/files.ts` | `POST /api/files/check-hashes`，body `{hashes: string[]}`，返回 `{existing: {hash: {fileName, size}}}`（仅命中项，上限 100 个/次） |
| 后端 | `src/routes/reports.ts` | 报告生成：按索引合并新上传文件（`inputFile{i}`）与秒传引用（`dedupRefs` JSON，`{索引: hash}`），保证与 `inputHashes` 严格对齐；生成成功后新文件收编进 CAS |
| 后端 | `src/routes/ai.ts` | AI 分析：命中时表单只传 `dedupHash`（64 位小写 hex），后端从 CAS 读内容 |
| 前端 | `src/services/api.ts` | `checkFileHashes()` 预检查；`apiGenerateReport` 支持 `dedupIndices` 参数 |
| 前端 | `src/components/report/BatchFileUploader.tsx` | hash 完成后自动预检查，命中显示「⚡秒传」徽标 |
| 前端 | `src/components/report/AIAnalysisPanel.tsx` | 分析前算 hash 预检查，命中只传 `dedupHash` 并 toast 提示 |

### 运维注意

- 清理范围严格限定 `data/uploads/`；`scripts/`、`reports/`、`knowledge-base/`、`templates/` 永久存储不受影响。
- CAS 记录被命中使用时刷新 `last_used_at`，即"常用文件永不过期"。
- 索引在但文件丢失时，`lookup` 会自动清理该索引行并返回 null，上传方回退为正常上传。

---

## 二、AI 支持包整包智能分析

### 功能说明

AI 智能分析可直接分析服务器厂商支持包（如华为 BMC 一键收集包 `.tar.gz`）：后端自动解压、按优先级规则挑选关键文本日志（告警/事件/诊断优先），在 150KB 文本预算内拼接结构化上下文交给 AI，输出整机健康诊断报告。

### 架构与关键文件

| 层 | 文件 | 说明 |
|---|---|---|
| 后端 | `src/services/archiveAnalysisService.ts` | `isArchiveFile()`（扩展名+magic bytes 双判定）、`extractEntries()`（tar/tar.gz/tgz/gz 用 npm `tar`，zip 用 `adm-zip`，单 gz 用 zlib）、`smartSelect()`（三层优先级筛选） |
| 后端 | `src/services/aiPrompts.ts` | 新增 `support`（整机支持包）类别与专用提示词 |
| 后端 | `src/routes/ai.ts` | analyze-file：压缩包走解压+筛选分支；上传上限 50MB |
| 前端 | `src/components/report/AIAnalysisPanel.tsx` | 类别页签含「整机支持包」；压缩包文件名旁显示「整包智能分析」徽标 |

### 智能筛选规则（smartSelect）

- **P0 告警/事件/诊断**（路径匹配 `event|alarm|sel|diagnos|fault|fdm_log`）：全取；单文件超 64KB 取头 48KB + 尾 16KB
- **P1 关键系统日志**（`kernel|raid|storage|sensor|bmc|system|power|fan|temp|osdump|linux`）：取尾部 32KB（最新日志在末尾）
- **P2 其他文本**：取尾部 8KB
- 总预算 150KB，按 P0→P1→P2 填充；输出含「支持包智能筛选摘要」头部
- 文本判定：扩展名白名单 + 无扩展名文件前 8KB 抽样（NUL 字节/控制字符比例）
- 安全限制：路径穿越过滤、条目 ≤2000、单文件 ≤32MB、总解压 ≤200MB

### 依赖变化

- 新增 `adm-zip`（zip 解压，零原生依赖）与 `@types/adm-zip`
- `express-rate-limit` 补为正式依赖（此前被 index.ts 使用但未声明，靠间接依赖提升存在）

### 实测基准

样本 `samples/2288HV6_..._20260507-0829.tar.gz`（2.8MB / 463 文件）：选中 38 个关键文件（153KB），AI 正确识别 DISK0 磁盘故障、RAID 阵列失效、逻辑盘降级三条真实告警。

---

## 三、AI 分析双模式开关 + 用户补充提示

### 功能说明

- AI 智能分析面板有「输入模式」开关：**单文件模式（默认）** ⇄ **支持包模式**。
- 文件类型与模式不匹配时自动切换模式并 toast 提示；切到支持包模式时类别自动设为「整机支持包」，切回时恢复之前类别。
- 「补充提示（可选）」可折叠文本框：用户给 AI 方向性背景信息（如"已确认硬盘故障，请重点检查存储子系统"），与自定义提示词（整份替换）互不冲突。

### 前后端契约

- 前端表单新增 `userHint` 字段（≤2000 字符，超限静默截断）。
- 后端 `analyze-file` 在 `getPrompt()` 结果后追加 `\n\n## 用户补充提示\n...` 段落。

---

## 四、设计与实现文档索引

| 文档 | 路径 |
|---|---|
| 秒传去重 + 保留清理设计 | `smart-report-server/docs/superpowers/specs/2026-08-04-file-retention-dedup-design.md` |
| 秒传去重 + 保留清理计划 | `smart-report-server/docs/superpowers/plans/2026-08-04-file-retention-dedup.md` |
| 支持包分析设计 | `smart-report-server/docs/superpowers/specs/2026-08-04-ai-archive-analysis-design.md` |
| 支持包分析计划 | `smart-report-server/docs/superpowers/plans/2026-08-04-ai-archive-analysis.md` |
| 双模式 + 补充提示设计 | `smart-report-server/docs/superpowers/specs/2026-08-04-ai-analysis-mode-toggle-design.md` |

## 五、提交记录

**后端（smart-report-server）**

```
d145e11 feat(ai): analyze-file 支持 userHint 用户补充提示
6d2f4e3 feat(ai): analyze-file 接入支持包整包分析 + 新增整机支持包类别
f2db036 feat(ai): 支持包内存解压与三层优先级智能筛选服务
3e1aa3a feat(ai): 分析上传接入去重存储（dedupHash 秒传）
78004e0 feat(reports): 报告生成支持秒传引用（dedupRefs）并收编输入文件
c6c6ac1 feat(files): 临时文件保留清理调度 + 开放文件保留天数设置
924b715 feat(files): 内容寻址去重存储 + check-hashes 预检查端点
```

**前端（smart-report-tool）**

```
7ff1cf0 feat(ai-analysis): 单文件/支持包双模式开关 + 用户补充提示框
35d9f77 feat(ai-analysis): 整机支持包类别 + 压缩包自动切换 + 50MB 上限
ccbad44 feat(ai-analysis): AI 文件分析支持秒传
974342d feat(report): 报告创建支持秒传（hash 预检查 + dedupRefs）
```

## 六、接手建议

1. **验证环境**：后端 `cd smart-report-server && npx tsx src/index.ts`（3001 端口），前端 `cd smart-report-tool && npm run dev`（5173 端口）；默认账号 admin / admin123。
2. **改筛选策略**：调 `archiveAnalysisService.ts` 顶部的 `P0_RE/P1_RE/TOTAL_BUDGET` 等常量即可，无需动其他代码。
3. **接入新压缩格式**：在 `extractEntries()` 加分支；RAR 目前明确返回不支持。
4. **项目无单元测试框架**：验证惯例是 `npx tsc --noEmit`（双端）+ `npx vite build`（前端）+ curl / 浏览器实测，改动后请至少跑这三项。
5. **samples/ 目录**（工作区根）存有华为 BMC 支持包样本，可作回归测试基准。
